import { View, Text } from "react-native";

export default function Reels() {
    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Reels place holder, check messages</Text>
        </View>
    );
}